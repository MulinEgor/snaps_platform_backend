services:
  api:
    container_name: api
    build: .
    command: >
        sh -c " \
        python -m prisma generate &&  \
        uvicorn api.main:app --host ${API_HOST} --port ${API_PORT}"
    volumes:
      - .:/app
    env_file:
      - .env
    ports:
      - "${API_PORT}:${API_PORT}"
  